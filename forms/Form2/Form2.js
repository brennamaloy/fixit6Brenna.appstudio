const removePets = petNames.indexOf("Vinny")
if (index > -1) { petNames.splice(index, 1) }

alert('Vinnys name was removed')

const removePets = petNames.indexOf("Marty")
if (index > -1) { petNames.splice(index, 3) }

alert('Marty's name was removed') 

